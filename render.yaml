services:
  - type: web
    name: phone-check
    runtime: python
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.11"
    buildCommand: pip install .
    startCommand: uvicorn phone_check.api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
